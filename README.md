# ⚙️ Простий Gulp SCSS Проект

Мінімальний проєкт для вивчення **Gulp** з **SCSS** — легкий старт для автоматизації фронтенд-розробки.

## 🚀 Що це робить?

✅ **SCSS → CSS** — компілює SCSS у звичайний CSS  
✅ **Автопрефікси** — додає вендорні префікси (*-webkit-*, *-moz-* тощо)  
✅ **Live Reload** — автоматично оновлює сторінку при змінах  
✅ **Мінімізація** — стискає CSS для швидшого завантаження  

## ⚙️ Встановлення

1. Встановіть [Node.js](https://nodejs.org/)  
2. Завантажте або клонуйте репозиторій:
   ```bash
   git clone https://github.com/your-username/gulp-scss-starter.git
3. Встановіть залежності: **npm install**
5. Для разробки: **npm run dev**
   - Компілює SCSS у CSS
   - Запускає локальний сервер на http://localhost:3000
   - Автоматично оновлює сторінку при змінах файлів
7. Для збірки (Production): **npm run build**
    - Компілює SCSS
    - Додає автопрефікси
    - Мінімізує CSS
      
## Як працювати?

1. Відкрийте файл src/scss/style.scss
2. Пишіть свої SCSS стилі
3. Збережіть файл
Перевірте результат у браузері — зміни оновляться автоматично

